<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jovines.lbs_server.dao.HighqualityuserDao">

    <resultMap type="com.jovines.lbs_server.entity.Highqualityuser" id="HighqualityuserMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="user" column="user" jdbcType="INTEGER"/>
        <result property="jointime" column="joinTime" jdbcType="TIMESTAMP"/>
        <result property="goodreason" column="goodReason" jdbcType="VARCHAR"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="HighqualityuserMap">
        select
          id, user, joinTime, goodReason
        from lbs.highQualityUser
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="HighqualityuserMap">
        select
          id, user, joinTime, goodReason
        from lbs.highQualityUser
        limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="HighqualityuserMap">
        select
          id, user, joinTime, goodReason
        from lbs.highQualityUser
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="user != null">
                and user = #{user}
            </if>
            <if test="jointime != null">
                and joinTime = #{jointime}
            </if>
            <if test="goodreason != null and goodreason != ''">
                and goodReason = #{goodreason}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into lbs.highQualityUser(user, joinTime, goodReason)
        values (#{user}, #{jointime}, #{goodreason})
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update lbs.highQualityUser
        <set>
            <if test="user != null">
                user = #{user},
            </if>
            <if test="jointime != null">
                joinTime = #{jointime},
            </if>
            <if test="goodreason != null and goodreason != ''">
                goodReason = #{goodreason},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from lbs.highQualityUser where id = #{id}
    </delete>

</mapper>